# 객체란?
- 자바스크립트는 객체 기반의 언어이며 사실상 모든 것이 객체다
- 원시 타입은 불변하는 값이지만 객체는 변경이 가능한 값이다
## 프로퍼티와 메서드
- 프로퍼티는 객체의 상태를 나타내는 값
- 메서드는 프로퍼티를 참조하고 조작할 수 있는 동작
- 객체는 둘 다 포함하고 있어서 상태와 동작을 하나의 단위로 구조화할 수 있어 유용하다

## 자바스크립트에서 객체와 함수
- 자바스크립트 객체는 함수와 밀접한 연관 관계를 맺는다
- 함수로 객체를 생성할 수 있고, 함수 자체가 객체이기도 하다.
